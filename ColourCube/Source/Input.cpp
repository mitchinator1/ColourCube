#include "Input.h"
#include <iostream>

Input::Input()
{

}
/*
void Input::Update(GLFWwindow* window)
{
	for (unsigned int i = 0; i < m_Keys.size(); i++)
		m_Keys[i].Pressed = glfwGetKey(window, m_Keys[i].ID);
}
*/
/*
Key Input::GetKey(int key)
{
	for (unsigned int i = 0; i < m_Keys.size(); i++)
		if (m_Keys[i].ID == key)
		{
			std::cout << m_Keys[i].ID << std::endl;
			return m_Keys[i];
		}
	return { 0 };
}
*/